{
  "name": "tourx-mock-server",
  "version": "1.0.0",
  "private": true,
  "description": "Mock backend for the TourX Expo app: json-server CRUD store + a thin Express layer that simulates auth and the AI/mapping services described in the TourX proposal.",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "google-auth-library": "^10.9.0",
    "json-server": "^0.17.4",
    "jsonwebtoken": "^9.0.2",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "nodemon": "^3.1.4"
  }
}
